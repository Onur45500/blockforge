## Summary

<!-- What changed and why. -->

## Test plan

- [ ] `npx pnpm test`
- [ ] `npx pnpm typecheck`
- [ ] Template change: `rbxtsc` + `npm run validate:world` / `validate:refs` in `packages/project-template`
- [ ] Prompt / hook change: `npx pnpm evals:dry` (and `evals:run` if you have Claude logged in)
- [ ] UI change: described what you clicked in the desktop app

## Notes

<!-- Screenshots for UI. Redact Settings keys. -->
