# Terminal spike (historical)

Early Electron + `node-pty` experiment. **The real product is `apps/desktop`.**

Kept only as a small reference for PTY launch on Windows. Do not add features here. Prefer deleting this package in a later cleanup if CI/install cost is annoying.

```bash
npx pnpm spike:terminal
```
