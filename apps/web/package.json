{
  "name": "@blockforge/web",
  "version": "0.1.0",
  "private": true,
  "description": "Blockforge marketing site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('web: no unit tests')\""
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.6"
  }
}
