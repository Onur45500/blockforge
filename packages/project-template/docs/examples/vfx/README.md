# VFX burst pad (reference only)

Copy `VfxPad.model.json` into `world/` and `burst.server.ts` into `src/server/`.

Keep Rate low; burst with `Emit`, then leave the emitter disabled. Do not invent a particle texture `rbxassetid://`.
