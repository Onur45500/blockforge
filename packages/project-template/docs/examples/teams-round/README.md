# Teams + timed round (reference only)

Copy `TeamPads.model.json` into `world/` and `teams-round.server.ts` into `src/server/`.

Two pads (`TeamPadRed` / `TeamPadBlue`) — **not** extra enabled SpawnLocations. 60s round, 10s intermission, `leaderstats/Points` +1 per survived round. Map pick + timer HUD: [arena-maps](../arena-maps/). Combat: [combat-raycast](../combat-raycast/).
