# NPC patrol (reference only)

Copy `npc.server.ts` into `src/server/` and `NpcMarker.model.json` into `world/`.

MVP: an anchored Part hops between 2–3 points. Path around obstacles toward a player: [pathfinding](../pathfinding/).

Done when something visible moves in Play.
