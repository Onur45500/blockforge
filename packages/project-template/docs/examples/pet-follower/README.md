# Pet follower (reference only)

Copy `PetFollower.model.json` into `world/` and `pet-follower.server.ts` into `src/server/`.

Touch `PetGrantPad` → server clones `PetMarker` (Anchored) and `PivotTo`s it behind HRP. Not hatching, trading, or a second currency.
