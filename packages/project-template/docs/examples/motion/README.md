# Motion bob (reference only)

Copy `BobMarker.model.json` into `world/` and `bob.server.ts` into `src/server/`.

Server tweens replicate. Do not Heartbeat-tween hundreds of parts. Keep decorative parts **Anchored**.
