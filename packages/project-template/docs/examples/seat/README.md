# Sit bench (non-script)

Copy `Bench.model.json` into `world/`. A `Seat` is enough — no TypeScript required for sitting.

Keep it **Anchored**. Place it on the spawn platform so the player can walk to it.
