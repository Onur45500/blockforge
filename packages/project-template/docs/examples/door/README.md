# ProximityPrompt door (reference only)

Copy `ShopDoor.model.json` into `world/` and `door.server.ts` into `src/server/`.

The prompt is created in TypeScript (not world JSON) so the door stays a plain anchored Part.
