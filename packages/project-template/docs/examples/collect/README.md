# Click collect (reference only)

Copy `CollectPad.model.json` into `world/` and `collect.server.ts` into `src/server/`.

Server grants coins (never trust a client-sent amount). Pair with [leaderstats](../leaderstats/).
