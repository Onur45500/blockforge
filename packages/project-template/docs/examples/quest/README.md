# One-shot quest (reference only)

Copy `Quest.model.json` into `world/` and `quest.server.ts` into `src/server/`. Needs coins ([leaderstats](../leaderstats/)).

ProximityPrompt on `QuestNpc` → touch `QuestGoal` → +10 coins once (server flag). No dialogue tree.
