# Lighting (reference only)

- `Lantern.model.json` — PointLight accent (copy into `world/`).
- `day-night.server.ts` — slow `Lighting.ClockTime` cycle (copy into `src/server/`).

Keep lanterns **Anchored**. Do not invent light cookie / texture ids.
