# Studio tools

Versioned Luau libraries synced by Rojo to `ReplicatedStorage.Blockforge`.

| File | Module |
|------|--------|
| `MapToolkit.luau` | Grounding, scatter, z-fight / overlap / rig / off-screen UI audit |

```bash
npm run map-toolkit
```

Agents **require** the module after sync. Never paste this source into `execute_luau`.
