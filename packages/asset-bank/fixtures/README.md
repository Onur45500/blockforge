# Asset fixtures

Used by the Milestone 0 pipeline spike and unit tests.

| File | Purpose |
|------|---------|
| `sample.png` | 1×1 PNG for Decal upload tests |
| `sample.fbx` | Minimal valid FBX cube for Model preview + upload path wiring |

Replace `sample.fbx` with real CC0 meshes (e.g. from Kenney) before expecting distinctive shapes or a successful live Roblox model moderation pass.
