# Low-poly Nature style pack

Copy preferred prefabs into your project's `world/` and set active style in
`.blockforge/style-pack.json`:

```json
{ "id": "lowpoly-nature", "version": "1.0.0" }
```

Agent bootstrap / hooks should inject `agentPrompt` from `pack.json`.
